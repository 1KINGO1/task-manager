<UserControl x:Class="TaskManager.WpfApp.Views.ProjectEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24" MaxWidth="640" HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="{Binding FormTitle}"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Margin="0,0,0,16" />

            <StackPanel Grid.Row="1" Margin="0,0,0,14">
                <TextBlock Text="Назва*" Foreground="#AAA" Margin="0,0,0,4" />
                <TextBox Padding="8,6"
                         Background="#2D2D30"
                         Foreground="White"
                         BorderBrush="#3F3F46"
                         CaretBrush="White"
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,0,0,14">
                <TextBlock Text="Опис" Foreground="#AAA" Margin="0,0,0,4" />
                <TextBox Padding="8,6"
                         Background="#2D2D30"
                         Foreground="White"
                         BorderBrush="#3F3F46"
                         CaretBrush="White"
                         MinHeight="110"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Grid.Row="3" Margin="0,0,0,18">
                <TextBlock Text="Тип проєкту" Foreground="#AAA" Margin="0,0,0,4" />
                <ComboBox MinWidth="240"
                          ItemsSource="{Binding ProjectTypes}"
                          SelectedItem="{Binding SelectedType}" />
            </StackPanel>

            <StackPanel Grid.Row="4" Orientation="Horizontal">
                <Button Content="Зберегти"
                        Padding="14,8"
                        Margin="0,0,8,0"
                        Background="#0E7C3A"
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Command="{Binding SaveCommand}" />
                <Button Content="Скасувати"
                        Padding="14,8"
                        Background="#3E3E42"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Command="{Binding CancelCommand}" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
